# toomuCh

**toomuCh** — это минималистичный анонимный имиджборд, разработанный на языке Go. Проект представляет собой упрощенный аналог классических борд, где пользователи могут делиться своими мыслями на одной общей доске.

Создан как pet-проект для изучения экосистемы Golang и практики построения чистой архитектуры.

## 🚀 Особенности

- **Анонимность**: Публикация записей без необходимости регистрации или авторизации.
- **Простота**: Единая общая доска для всех сообщений.
- **Записи**: Возможность создания постов с заголовком и основным текстом.
- **Конфигурация**: Гибкая настройка через переменные окружения и YAML файлы (Viper).
- **Логирование**: Структурированное логирование событий с помощью Uber Zap.
- **Docker-ready**: Готовые конфигурации для быстрого развертывания в контейнерах.

## 🛠 Технологический стек

- **Язык**: [Go (Golang)](https://golang.org/) v1.23+
- **Роутинг**: [Chi](https://github.com/go-chi/chi) — легковесный и производительный HTTP роутер.
- **ORM**: [GORM](https://gorm.io/) — удобное взаимодействие с базой данных.
- **База данных**: [PostgreSQL](https://www.postgresql.org/).
- **Конфигурация**: [Viper](https://github.com/spf13/viper).
- **Логирование**: [Zap](https://github.com/uber-go/zap).
- **Контейнеризация**: Docker & Docker Compose.

## 📦 Установка и запуск

### Предварительные требования

- Установленный [Go](https://golang.org/doc/install) (версия 1.23 или выше).
- Установленный [Docker](https://docs.docker.com/get-docker/) и [Docker Compose](https://docs.docker.com/compose/install/).

### Запуск через Docker (рекомендуется)

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/KirillLich/toomuCh.git
   cd toomuCh
   ```

2. Создайте файл `.env` на основе примера:
   ```bash
   cp .env.example .env
   ```

3. Запустите проект:
   ```bash
   docker-compose up --build
   ```

Приложение будет доступно по адресу `http://localhost:8080` (или по порту, указанному в ваших настройках).

### Локальный запуск

1. Убедитесь, что PostgreSQL запущен и доступен.
2. Настройте параметры подключения в `.env` или `config/config.yaml`.
3. Запустите сервер:
   ```bash
   go run cmd/main.go
   ```

## 📂 Структура проекта

Проект следует принципам чистой архитектуры:

- `cmd/` — точка входа (main).
- `config/` — логика загрузки и хранения конфигураций.
- `internal/` — внутренняя логика приложения:
    - `app/` — сборка и инициализация зависимостей.
    - `domain/` — сущности и интерфейсы.
    - `repository/` — реализация работы с БД.
    - `service/` — бизнес-логика.
    - `transport/` — HTTP хендлеры.

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).
